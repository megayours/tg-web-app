function mint(yours.token, ft4.accounts.account, amount: integer) {
  require(yours.has_module(token, module_name), "Token does not support equippables");
  yours.mint(token, yours.balance_info(account, amount));
}
